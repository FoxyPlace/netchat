Set-Location 'C:\wamp64\www\netchat\netchat'; $includes=@('.php','.js','.css','.html','.htm','.sql','.md','.json','.txt','.yml','.yaml','.xml','.ps1','.ini','.env','.scss','.less'); $files=Get-ChildItem -Recurse -File | Where-Object { $includes -contains $_.Extension }; $extStats=@(); foreach ($extGroup in ($files | Group-Object Extension)) { $ext=$extGroup.Name; $count=$extGroup.Count; $lines=0; foreach ($f in $extGroup.Group) { try { $lines += (Get-Content $f.FullName -ErrorAction Stop).Count } catch {} } $extStats += [PSCustomObject]@{Extension=$ext; Files=$count; Lines=$lines} }; $extStats|Sort-Object Files -Descending|Format-Table -AutoSize; Write-Output '---LANGUAGE AGGREGATE---'; $extMap=@{'.php'='PHP'; '.js'='JavaScript'; '.css'='CSS'; '.html'='HTML'; '.htm'='HTML'; '.sql'='SQL'; '.md'='Markdown'; '.json'='JSON'; '.txt'='Text'; '.yml'='YAML'; '.yaml'='YAML'; '.xml'='XML'; '.ps1'='PowerShell'; '.ini'='Config'; '.env'='Config'; '.scss'='CSS'; '.less'='CSS'}; $langStats=@{}; foreach ($row in $extStats) { $ext=$row.Extension; $lang=$extMap[$ext]; if (-not $lang) { $lang='Other' } if (-not $langStats.ContainsKey($lang)) { $langStats[$lang]=@{Files=0;Lines=0} } $langStats[$lang].Files += $row.Files; $langStats[$lang].Lines += $row.Lines }; $langStats.GetEnumerator() | ForEach-Object { [PSCustomObject]@{Language=$_.Name; Files=$_.Value.Files; Lines=$_.Value.Lines} } | Sort-Object Lines -Descending | Format-Table -AutoSize